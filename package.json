{
  "name": "haziri",
  "version": "1.0.0",
  "description": "Geofenced Attendance Management System",
  "private": true,
  "scripts": {
    "dev": "npx -y concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install-all": "npm install --prefix server && npm install --prefix client"
  }
}
